# Prompt v1.2 — ANCLORA-GAA-001

Genera alertas operativas deduplicadas para `ai_runtime_degraded` y `seller_signal_source_degraded` usando el contrato cloud compartido. La UI debe mostrar metadata accionable y diferenciar claramente estos scopes de los guardrail blocks.

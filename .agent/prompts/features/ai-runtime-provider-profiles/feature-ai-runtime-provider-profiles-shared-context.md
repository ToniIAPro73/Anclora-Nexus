# Shared Context - ANCLORA-AIRP-001

- Goal: migrate Nexus runtime from legacy OpenAI/Anthropic coupling to Groq + Cloudflare.
- Current public `LLMService` methods must remain stable.
- v1 scope is backend + governance + env contracts.
- Frontend UI is out of scope; API introspection replaces UI surface.

# SKILL - ANCLORA-DLAB-001

## Objetivo

Construir `Data Lab Portal v1` como capa de acceso y lectura de inteligencia reutilizable.

## Flujo

1. reutilizar `MTIP` y `Source Observatory`
2. no duplicar backend si el contrato actual ya cubre la necesidad
3. mantener experiencia publica controlada y experiencia live solo para tenant autenticado
4. alinear `Private Estates` para que la entrada a `Data Lab` comunique el producto real
